{
  "name": "monitor-electron",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo 'Running basic Electron test...' && npx electron --version"
  },
  "dependencies": {
    "electron": "^34.2.0",
    "dockerode": "^4.0.4",
    "ssh-config": "^5.0.3"
  }
}
